# Migrations

Add Alembic migration scripts here.

Recommended first migrations:

1. Create all base tables
2. Seed `roles` with `Student`, `Staff`, `Technician`, `Admin`
3. Add indexes and constraints for reservation overlap and lookup performance

